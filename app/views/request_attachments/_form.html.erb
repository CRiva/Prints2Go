<%= form_for(request_attachment) do |f| %>
  <% if request_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(request_attachment.errors.count, "error") %> prohibited this request_attachment from being saved:</h2>

      <ul>
      <% request_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :request_id %>
    <%= f.number_field :request_id %>
  </div>

  <div class="field">
    <%= f.label :file %>
    <%= f.text_field :file %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
